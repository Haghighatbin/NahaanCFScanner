<div align="center">

**🌐 Language / زبان**

[![English](https://img.shields.io/badge/English-blue?style=for-the-badge)](README.md)
[![فارسی](https://img.shields.io/badge/فارسی-green?style=for-the-badge)](README_FA.md)

</div>

---

<div dir="rtl">
</div>

# اسکنر IP کلودفلر - نهان


یک رابط کاربری وب ساده برای تست IPهای کلودفلر با اتصالات VLESS. طراحی شده برای کاربران ایرانی جهت یافتن بهترین IPهای کلودفلر که با ISP آنها سازگار است.

## امکانات

- **پیکربندی آسان**: آپلود یا ویرایش مستقیم تنظیمات VLESS در مرورگر
- **تنظیمات از پیش آماده**: تنظیمات بهینه‌شده برای اینترنت کند/سریع
- **پیشرفت لحظه‌ای**: مشاهده پیشرفت اسکن در مراحل جمع‌آوری DNS، پینگ TCP و تست سرعت
- **نتایج نمابش داده شدهس**: نمودارهای تعاملی نشان‌دهنده توزیع سرعت و عملکرد اپراتورها
- **گزینه‌های خروجی**: دانلود نتایج به صورت JSON یا CSV
- **عیب‌یابی**: بررسی‌های سیستمی داخلی و تست IP تکی
- **تاریخچه اسکن**: نگهداری سوابق اسکن‌های قبلی

## نصب

### پیش‌نیازها

1. **پایتون ۳.۸ یا بالاتر**
2. **فایل باینری xray-core** (دانلود از https://github.com/XTLS/Xray-core/releases)
در فولدر converters موجود است

### مراحل نصب در ویندوز

1. **نصب پایتون** (اگر قبلاً نصب نشده)
   - دانلود از https://www.python.org/downloads/
   - هنگام نصب، گزینه "Add Python to PATH" را تیک بزنید

2. **دانلود xray-core**
   ```
   1. به آدرس زیر بروید: https://github.com/XTLS/Xray-core/releases/latest
   2. دانلود کنید: Xray-windows-64.zip
   3. فایل xray.exe را در مسیر converters/xray-core/xray.exe قرار دهید
   ```

3. **نصب پیشنیازها**
   ```bash
   pip install -r requirements_streamlit.txt
   ```

4. **اجرای برنامه**
   ```bash
   streamlit run streamlit_app.py
   ```
   
   یا به سادگی روی `run_streamlit.bat` دوبار کلیک کنید (ویندوز)

## شروع سریع

### روش ۱: استفاده از فایل Batch (ویندوز)

1. روی `run_streamlit.bat` دوبار کلیک کنید
2. مرورگر شما به صورت خودکار باز می‌شود
3. پیکربندی و اسکن را شروع کنید!

### روش ۲: خط فرمان

```bash
streamlit run streamlit_app.py
```

برنامه در مرورگر پیش‌فرض شما در آدرس `http://localhost:8501` باز می‌شود

## راهنمای کاربر

### ۱. پیکربندی

#### آپلود فایل تنظیمات
- نوار کناری ← "Upload config.yaml"
- فایل تنظیمات خود را بکشید و رها کنید یا مرور کنید

#### یا ویرایش مستقیم
- نوار کناری ← "Edit VLESS Configuration"
شناسه  UUID - نام سرور، هدر میزبان و سایر تنظیمات VLESS خود را وارد کنید

#### پیکربندی IP تست
- یک "IP تست معتبر" تنظیم کنید (پیش‌فرض: 173.245.49.235)
- این IP تأیید می‌کند که سرور VLESS شما به درستی کار می‌کند
- گزینه "Include test IP in scan" را تیک بزنید تا به لیست تست اضافه شود

### ۲. پارامترهای اسکن

#### انتخاب تنظیمات از پیش آماده
- **اینترنت کند (ایمن)**: بهترین گزینه برای شرایط اینترنت ایران
  - زمان انتظار اتصال: ۳۰ ثانیه
  - مدت تست: ۱۰ ثانیه
  - تعداد IP برای تست: ۱۰
  - توصیه شده برای اکثر کاربران

- **اینترنت سریع (تهاجمی)**: برای اتصالات پایدار
  - زمان انتظار اتصال: ۱۰ ثانیه
  - مدت تست: ۵ ثانیه
  - تعداد IP برای تست: ۲۰

- **سفارشی**: تنظیم دستی همه پارامترها

#### کنترل‌های پیشرفته
- زمان انتظار اتصال: مدت انتظار برای هر IP
- مدت تست سرعت: مدت زمان اندازه‌گیری سرعت دانلود
- تعداد IPها برای تست: چند IP با VLESS تست شوند
- زمان انتظار پینگ TCP: بررسی اولیه اتصال

### ۳. اجرای اسکن

1. روی **"START SCAN"** کلیک کنید

2. پیشرفت را در ۳ مرحله مشاهده کنید:
   - **مرحله ۱**: جمع‌آوری DNS (گردآوری IPهای کلودفلر)
   - **مرحله ۲**: اتصال TCP (تست دسترسی به پورت ۴۴۳)
   - **مرحله ۳**: تست سرعت VLESS (اندازه‌گیری عملکرد واقعی)

3. نتایج را به صورت لحظه‌ای مشاهده کنید:
   - نوارهای پیشرفت برای هر مرحله
   - جریان گزارش زنده نشان‌دهنده هر IP در حال تست
   - شمارنده‌های موفقیت/شکست

4. **تست سریع**: روی "Quick Test" کلیک کنید تا فقط ۵ IP تست شوند (سریع‌تر)

### ۴. مشاهده نتایج

به تب **"Results"** بروید:

- **برجسته‌سازی بهترین IP**: سریع‌ترین IP یافت شده
- **آمار**: میانگین سرعت، تأخیر، تعداد IPهای تست شده
- **جدول نتایج**: جدول قابل مرتب‌سازی از همه IPهای کارآمد
- **فیلترها**: فیلتر بر اساس سرعت، تأخیر یا اپراتور
- **نمودارها**:
  - هیستوگرام توزیع سرعت
  - تأخیر بر اساس اپراتور
  - نمودار پراکندگی سرعت در مقابل تأخیر

#### خروجی گرفتن از نتایج
- **Export JSON**: نتایج کامل با تمام جزئیات
- **Export CSV**: فرمت سازگار با اسپردشیت
- **Copy Best IP**: کپی سریع برای استفاده فوری

### ۵. عیب‌یابی و تست

به تب **"Diagnostics"** بروید:

#### بررسی سیستم
- روی "Run System Diagnostics" کلیک کنید
- موارد زیر را تأیید می‌کند:
  - وجود فایل باینری xray-core
  - معتبر بودن پیکربندی
  - در دسترس بودن پورت
  - اتصال به اینترنت
  - وضوح DNS

#### تست IP تکی
- هر IP کلودفلر را وارد کنید
- روی "Test IP" کلیک کنید
- نتایج فوری را بدون اسکن کامل ببینید

#### اعتبارسنج پیکربندی
- روی "Validate VLESS Configuration" کلیک کنید
- اطمینان از وجود همه فیلدهای ضروری
- بررسی خطاهای رایج پیکربندی

## عیب‌یابی

### «فایل باینری xray-core یافت نشد»

**راه حل:**
1.  از آدرس xray-core  دانلود کنید: https://github.com/XTLS/Xray-core/releases
2. فایل `xray.exe` را در مسیر `converters/xray-core/xray.exe` قرار دهید
3. برنامه را مجدداً راه‌اندازی کنید

### ⚠️ «پورت ۱۰۸۰ در حال استفاده است»

**راه حل:**
- برنامه سعی می‌کند پورت را به صورت خودکار آزاد کند
- یا پورت SOCKS را در پیکربندی (نوار کناری) تغییر دهید
- یا برنامه‌هایی که از پورت ۱۰۸۰ استفاده می‌کنند را به صورت دستی ببندید

### ❌ «همه IPها ناموفق بودند»

**دلایل احتمالی:**
- پیکربندی نادرست VLESS (UUID، نام سرور و غیره)
- مسدود شدن IPهای کلودفلر توسط ISP شما
- سرور VLESS خاموش یا غیرقابل دسترس است

**راه حل‌ها:**
1. ابتدا با IP تست معتبر (173.245.49.235) تست کنید
2. "System Diagnostics" را برای بررسی پیکربندی اجرا کنید
3. بررسی کنید که سرور VLESS شما قابل دسترس است
4. تنظیمات "اینترنت کند" را برای اطمینان بیشتر امتحان کنید

### 🛡️ هشدار فایروال ویندوز

**راه حل:**
- وقتی فایروال ویندوز پیام می‌دهد، روی "Allow Access" کلیک کنید
- xray.exe برای عملکرد صحیح نیاز به دسترسی شبکه دارد

### اسکن بسیار کند است

**راه حل:**
- از تنظیمات "اینترنت کند" استفاده کنید (از قبل بهینه شده)
- "تعداد IP برای تست" را به ۵ تا ۱۰ کاهش دهید
- زمان‌های انتظار را در کنترل‌های پیشرفته افزایش دهید

## 📊 درک نتایج

### کدهای اپراتور ISP

- **MCI**: شرکت ارتباطات سیار ایران (همراه اول)
- **MTN**: ایرانسل
- **MKH**: مخابرات (TCI)
- **TEST**: IP تستی که مشخص کرده‌اید
- سایرین: ISPهای منطقه‌ای (HWB، AST، SHT و غیره)

### تفسیر نتایج

- **سرعت دانلود**: بالاتر بهتر است (هدف: بیش از ۵ مگابایت بر ثانیه)
- **تأخیر**: کمتر بهتر است (هدف: کمتر از ۱۰۰ میلی‌ثانیه)
- **اپراتور**: نشان می‌دهد کدام ISP با آن IP بهتر کار می‌کند

### استفاده از نتایج

1. **بهترین IP**: نتیجه بالای لیست معمولاً بهترین انتخاب است
2. **تطابق اپراتور**: IPهایی که با ISP شما مطابقت دارند اغلب بهتر کار می‌کنند
3. **IPهای پشتیبان**: ۳ تا ۵ IP برتر را به عنوان جایگزین ذخیره کنید
4. **تست مجدد**: عملکرد می‌تواند روزانه تغییر کند، مرتباً اسکن کنید

## امنیت و حریم خصوصی

- **عملکرد لوکال**: همه تست‌ها روی رایانه شما اجرا می‌شوند
- **بدون جمع‌آوری داده**: هیچ داده‌ای به سرورهای خارجی ارسال نمی‌شود
- **حریم تنظیمات**: پیکربندی VLESS شما روی دستگاه شما می‌ماند
- **کنترل خروجی**: شما کنترل می‌کنید چه چیزی را خروجی بگیرید و به اشتراک بگذارید

## ساختار فایل‌ها

```
NahaanCFScanner/
├── streamlit_app.py              # رابط اصلی
├── streamlit_helpers.py          # توابع کمکی
├── main.py                       # موتور اصلی اسکنر
├── config/
│   ├── config.yaml               # پیکربندی VLESS شما
│   └── template_config_vless.json # قالب VLESS
├── converters/
│   └── xray-core/
│       └── xray.exe              # فایل باینری xray-core
├── results/
│   ├── list.json                 # IPهای جمع‌آوری شده
│   ├── sorted_list.txt           # IPهای مرتب شده بر اساس پینگ
│   └── vless_tested_list.json    # نتایج تست سرعت
├── cf_editor/
│   ├── cf_ip_rev2.py             # حل‌کننده DNS
│   ├── providers.json            # ارائه‌دهندگان DNS
│   └── list.json                 # پایگاه داده IP
└── requirements_streamlit.txt     # وابستگی‌ها
```

## نکات برای بهترین نتایج

1. **از تنظیمات اینترنت کند استفاده کنید**: قابل اعتمادترین گزینه برای ایران
2. **در ساعات غیرپیک تست کنید**: نتایج بهتر در شب
3. **اسکن منظم**: عملکرد IP روزانه تغییر می‌کند
4. **چند IP ذخیره کنید**: ۳ تا ۵ IP خوب را به عنوان پشتیبان نگه دارید
5. **تطابق اپراتور**: IPهایی که با ISP شما مطابقت دارند را ترجیح دهید
6. **ابتدا تست کنید**: قبل از اسکن کامل از "Quick Test" استفاده کنید

## سؤالات متداول

### س: چرا برخی IPها سریع‌تر از بقیه هستند؟

**پ:** IPهای مختلف کلودفلر مسیریابی متفاوتی به ایران دارند. برخی مسیرها محدود یا شلوغ هستند، برخی دیگر بهتر کار می‌کنند.

### س: هر چند وقت یکبار باید اسکن کنم؟

**پ:** هفتگی یا وقتی کاهش سرعت را متوجه می‌شوید. عملکرد IP در طول زمان تغییر می‌کند.

### س: آیا می‌توانم از این روی لینوکس استفاده کنم؟

**پ:** بله! برنامه روی ویندوز، لینوکس و macOS کار می‌کند. فقط از فایل باینری xray مناسب استفاده کنید.

### س: آیا استفاده از این امن است؟

**پ:** بله. این ابزار فقط IPهای عمومی کلودفلر را تست می‌کند. با این حال، همیشه مطمئن شوید که پیکربندی سرور VLESS شما امن است.

### س: چرا اسکن خیلی طول می‌کشد؟

**پ:** هر IP نیاز دارد به:
1. تست اتصال TCP (حدود ۲ ثانیه)
2. راه‌اندازی اتصال VLESS (حدود ۳ ثانیه)
3. تست سرعت (حدود ۵ تا ۱۰ ثانیه)

با ۲۰ IP، این ۳ تا ۵ دقیقه طول می‌کشد.

### س: آیا می‌توانم چند اسکن همزمان اجرا کنم؟

**پ:** خیر. فقط یک اسکن در هر زمان برای جلوگیری از تداخل پورت.

## پشتیبانی

اگر با مشکل مواجه شدید:

1. تب "🛠️ Diagnostics" را بررسی کنید
2. گزارش‌های اسکن را در تب "🔍 Scanner" مرور کنید
3. ابتدا "Test Single IP" را با IP تست معتبر امتحان کنید
4. بررسی کنید که سرور VLESS شما به صورت مستقل قابل دسترس است

## تاریخچه نسخه‌ها

- **نسخه ۲.۰** (۱۴۰۴-۱۱-۱۰): انتشار رابط Streamlit
  - رابط کاربری وب کاربرپسند
  - پیگیری پیشرفت لحظه‌ای
  - نمودارها و تجسم‌های تعاملی
  - عیب‌یابی داخلی

- **نسخه ۱.۰**: رابط خط فرمان
  - نسخه اسکریپت پایتون اصلی

## تقدیر و تشکر

- **اسکنر اصلی VMESS**: فرید واحد (https://github.com/vfarid)
- **مهاجرت به VLESS و رابط Streamlit**: نهانگارد
- **xray-core**: پروژه XTLS (https://github.com/XTLS/Xray-core)

## مجوز

برای استفاده شخصی. کمک به بهبود اتصال اینترنت برای کاربران در محیط‌های محدود.

---

**نسخه:** ۲.۰  
**آخرین به‌روزرسانی:** ۱۴۰۴-۱۱-۱۰  
**ساخته شده با:** پایتون، Streamlit، xray-core
